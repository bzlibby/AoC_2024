const testInput = `xmul(2,4)%&mul[3,7]!@^do_not_mul(5,5)+mul(32,64]then(mul(11,8)mul(8,5))`
// replace this with export from puzzle inputs directory later

/* Part One */

// find mul(...) in string
// confirm that ... between () is <integer>,<integer>
// if yes, do multiplication and add to multiplication total

function findInstructions(corrupted){
  const decodedData = []

  // something something regex

  return decodedData;
}

function executeInstructions(decoded){
  let multiplicationTotal = 0;

  // ez (hopefully)

  return multiplicationTotal;
}

// console.log(executeInstructions(testInput))